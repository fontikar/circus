language: R
dist: focal
os: linux
warnings_are_errors: true
addons:
  apt:
    packages:
      - libmagick++-dev
      - libnode-dev
r_binary_packages:
  - brms
  - covr
  - devtools
  - MCMCglmm
after_success:
  - Rscript -e 'library(covr); codecov()'
  - bash <(curl -s https://codecov.io/bash)
